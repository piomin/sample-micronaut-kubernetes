micronaut:
  application:
    name: organization-service
  config-client:
    enabled: true

kubernetes:
  client:
    config-maps:
      labels:
        - app: organization
    secrets:
      enabled: true
      labels:
        - app: organization