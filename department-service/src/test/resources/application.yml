mongodb:
  database: test
  collection: department

micronaut:
  netty:
    event-loops:
      default:
        num-threads: 3
      httpclient:
        num-threads: 5
  http:
    client:
      read-timeout: 5s
      event-loop-group: httpclient
