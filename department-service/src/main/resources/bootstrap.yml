micronaut:
  application:
    name: department-service
  config-client:
    enabled: true

kubernetes:
  client:
    config-maps:
      labels:
        - app: department
    secrets:
      enabled: true
      labels:
        - app: department